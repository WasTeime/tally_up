import 'package:flutter/material.dart';

class ChequeTableRowWidget extends StatelessWidget {
  const ChequeTableRowWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
